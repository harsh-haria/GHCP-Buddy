{
  "name": "dynamic-preprocessor",
  "displayName": "GHCP Buddy",
  "description": "Intercepts prompts, transforms them using a fast model (optional), and executes them on the main model.",
  "publisher": "HarshHaria",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.90.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/harsh-haria/GHCP-Buddy.git"
  },
  "categories": [
    "AI",
    "Chat"
  ],
  "icon": "logo.ico",
  "activationEvents": [],
  "main": "./extension.js",
  "contributes": {
    "chatParticipants": [
      {
        "id": "chat.preProcessor",
        "name": "preProcessor",
        "fullName": "Pre-Processor Agent",
        "description": "Pre-processes prompts before sending to the main model.",
        "isSticky": true
      }
    ],
    "configuration": {
      "title": "Pre-Processor Agent",
      "properties": {
        "preProcessor.instructions": {
          "type": "string",
          "default": "",
          "description": "Instructions for the pre-processor. Leave empty to skip pre-processing."
        },
        "preProcessor.fastModelFamily": {
          "type": "string",
          "default": "gpt-4o-mini",
          "description": "The family of the fast model to use for pre-processing."
        }
      }
    }
  },
  "dependencies": {
    "@vscode/vsce": "^3.7.1"
  }
}
